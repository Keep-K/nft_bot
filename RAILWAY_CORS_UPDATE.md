# Railway CORS 환경 변수 업데이트 가이드

## 🚀 즉시 해결 방법

Railway 대시보드에서 다음 환경 변수들을 업데이트하세요:

### 1. CORS_ORIGIN 업데이트

**옵션 A: Firebase Hosting만 허용 (빠른 해결)**
```
CORS_ORIGIN=https://nft-binacealpha.web.app
```

**옵션 B: 여러 도메인 허용 (개발 + 프로덕션)**
```
CORS_ORIGIN=http://localhost:3000,https://nft-binacealpha.web.app,https://nft-binacealpha.firebaseapp.com
```

### 2. SIWE_DOMAIN 업데이트
```
SIWE_DOMAIN=nft-binacealpha.web.app
```

### 3. SIWE_URI 업데이트
```
SIWE_URI=https://nft-binacealpha.web.app
```

## 📋 업데이트 단계

1. Railway 대시보드 접속: https://railway.app
2. 프로젝트 선택 → 백엔드 서비스 클릭
3. **Variables** 탭 클릭
4. 각 환경 변수를 찾아서 값 업데이트
5. 자동 재배포 대기 (약 1-2분)

## ✅ 확인 방법

업데이트 후:

1. 브라우저에서 `https://nft-binacealpha.web.app` 접속
2. 개발자 도구 (F12) → Network 탭
3. 지갑 연결 시도
4. `/auth/nonce` 요청이 **200 OK**로 성공하는지 확인
5. CORS 오류가 사라졌는지 확인

## 🔧 코드 수정 사항

백엔드 코드가 이미 여러 origin을 지원하도록 수정되었습니다:
- `CORS_ORIGIN` 환경 변수를 쉼표로 구분하여 여러 도메인 허용 가능
- 예: `http://localhost:3000,https://nft-binacealpha.web.app`

## ⚠️ 주의사항

- 환경 변수 업데이트 후 서비스가 자동으로 재배포됩니다
- 재배포 중에는 일시적으로 서비스가 중단될 수 있습니다
- 재배포 완료까지 약 1-2분 소요됩니다

